apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.appName }}-backend
  description: ${{ values.description }}
  annotations:
    github.com/project-slug: ccl-labs/${{ values.appName }}-backend
spec:
  type: service
  lifecycle: production
  owner: app-developers
