# platform-gitops/platform/goldilocks/application.yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: goldilocks
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "20"
spec:
  project: default
  sources:
    - repoURL: https://charts.fairwinds.com/stable
      chart: goldilocks
      targetRevision: 10.3.0
      helm:
        valueFiles:
          - $values/platform/goldilocks/values.yaml
    - repoURL: https://github.com/okccl/platform-gitops.git
      targetRevision: main
      ref: values
    - repoURL: https://github.com/okccl/platform-gitops.git
      targetRevision: main
      path: platform/goldilocks/app-config
  destination:
    server: https://kubernetes.default.svc
    namespace: goldilocks
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
