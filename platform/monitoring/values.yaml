grafana:
  ingress:
    enabled: false
  # 管理者パスワード（ポートフォリオ用途のため固定値）
  adminPassword: admin
  grafana.ini:
    security:
      allow_embedding: true
    auth.anonymous:
      enabled: true
      org_role: Viewer
  additionalDataSources:
    - name: Loki
      type: loki
      uid: loki
      url: http://loki.logging.svc.cluster.local:3100
      isDefault: false
      jsonData:
        derivedFields:
          - matcherRegex: "traceID=(\\w+)"
            name: TraceID
            url: "$${__value.raw}"
            datasourceUid: "P214B5B846CF3925F"
    - name: Tempo
      type: tempo
      uid: tempo
      url: http://tempo.tracing.svc.cluster.local:3200
      isDefault: false
      jsonData:
        httpMethod: GET
prometheus:
  prometheusSpec:
    # すべてのnamespaceのServiceMonitorを検出
    serviceMonitorSelectorNilUsesHelmValues: false
    podMonitorSelectorNilUsesHelmValues: false
    retention: 7d
    resources:
      requests:
        memory: 512Mi
        cpu: 200m
      limits:
        memory: 1Gi
        cpu: 500m
alertmanager:
  alertmanagerSpec:
    # AlertmanagerConfig の namespace スコープ強制を無効化（全 namespace のアラートを受け取るため）
    alertmanagerConfigMatcherStrategy:
      type: None
    resources:
      requests:
        memory: 64Mi
        cpu: 50m
      limits:
        memory: 128Mi
        cpu: 100m
prometheusOperator:
  admissionWebhooks:
    enabled: false
    patch:
      enabled: false
  tls:
    enabled: false
